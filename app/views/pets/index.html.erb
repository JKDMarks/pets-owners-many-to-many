<div class="center jumbotron">
  <h1>Pets Index</h1>

  <div class="names-list">
    <% @pets.each do |pet| %>
      <span><%= link_to pet.name, pet %></span>
    <% end %>
  </div>

  <p><%= button_to "New Pet", new_pet_path, method: :get %></p>
</div>
